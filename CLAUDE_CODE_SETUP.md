# Claude Code Setup Guide

Цей посібник пояснює як налаштувати Electricity Shutdowns MCP Server для роботи з Claude Code.

## Передумови

1. Python 3.10 або вище встановлено
2. Claude Code встановлено ([інструкції](https://docs.claude.com/en/docs/claude-code))
3. Проект налаштовано згідно з [README.md](README.md)

## Метод 1: Використання mcp.json (рекомендовано для розробки)

Цей метод підходить для локальної розробки та тестування.

### Крок 1: Переконайтеся що віртуальне середовище активоване

```bash
cd electricity_shutdowns_mcp
source venv/bin/activate  # На Linux/macOS
# або
venv\Scripts\activate     # На Windows
```

### Крок 2: Запустіть MCP сервер

```bash
python -m src.server
```

Сервер очікує на вхідні дані через stdio (стандартний input/output).

### Крок 3: Використання з Claude Code

Якщо у вашому проекті є файл `mcp.json`, Claude Code автоматично виявить його і запропонує використати MCP сервер.

## Метод 2: Глобальна конфігурація Claude Desktop

Цей метод підходить для постійного використання з Claude Desktop.

### Крок 1: Знайдіть файл конфігурації Claude Desktop

**macOS:**
```
~/Library/Application Support/Claude/claude_desktop_config.json
```

**Windows:**
```
%APPDATA%\Claude\claude_desktop_config.json
```

**Linux:**
```
~/.config/Claude/claude_desktop_config.json
```

### Крок 2: Додайте конфігурацію MCP сервера

Відкрийте `claude_desktop_config.json` і додайте наступну конфігурацію:

```json
{
  "mcpServers": {
    "electricity-shutdowns": {
      "command": "/ПОВНИЙ/ШЛЯХ/ДО/ПРОЕКТУ/venv/bin/python",
      "args": [
        "-m",
        "src.server"
      ],
      "cwd": "/ПОВНИЙ/ШЛЯХ/ДО/ПРОЕКТУ/electricity_shutdowns_mcp",
      "env": {}
    }
  }
}
```

**Важливо:** Замініть `/ПОВНИЙ/ШЛЯХ/ДО/ПРОЕКТУ/` на реальний шлях до вашого проекту.

**Приклад для macOS:**
```json
{
  "mcpServers": {
    "electricity-shutdowns": {
      "command": "/Users/username/projects/electricity_shutdowns_mcp/venv/bin/python",
      "args": [
        "-m",
        "src.server"
      ],
      "cwd": "/Users/username/projects/electricity_shutdowns_mcp",
      "env": {}
    }
  }
}
```

### Крок 3: Перезапустіть Claude Desktop

Після збереження конфігурації, перезапустіть Claude Desktop, щоб зміни набули чинності.

## Перевірка роботи

### 1. Перевірте доступність tools

Після підключення MCP сервера, ви побачите доступні інструменти:
- `check_outage_schedule` - Перевіряє графік відключень
- `get_next_outage` - Знаходить наступне відключення
- `get_outages_for_day` - Отримує відключення для конкретного дня
- `set_address` - Налаштовує адресу

### 2. Налаштуйте адресу

Спочатку налаштуйте свою адресу:

```
Налаштуй мою адресу: м. Дніпро, Просп. Миру, буд. 4
```

Claude Code викличе `set_address` з правильними параметрами.

### 3. Перевірте графік

```
Перевір графік відключень електроенергії
```

Claude Code викличе `check_outage_schedule` і покаже вам результат.

## Troubleshooting

### Проблема: MCP сервер не запускається

**Рішення:**
1. Переконайтеся що всі залежності встановлені: `pip install -r requirements.txt`
2. Перевірте що Playwright встановлено: `playwright install chromium`
3. Перевірте шляхи у конфігурації (повинні бути абсолютні)

### Проблема: "Address not configured"

**Рішення:**
Спочатку налаштуйте адресу через `set_address`:
```
Налаштуй адресу: м. Дніпро, Просп. Миру, буд. 4
```

### Проблема: Помилка при парсингу сайту

**Рішення:**
1. Переконайтеся що адреса вказана правильно (з префіксами м., Просп., Вул.)
2. Перевірте що сайт ДТЕК доступний: https://www.dtek-dnem.com.ua/ua/shutdowns
3. Спробуйте використати `force_refresh: true` для примусового оновлення

## Приклади використання

### Базове використання

```
Я: Налаштуй мою адресу: м. Кривий Ріг, Вешенська, буд. 8

Claude: [Викликає set_address]
✓ Адресу збережено: м. Кривий Ріг, Вешенська, буд. 8

Я: Перевір графік відключень

Claude: [Викликає check_outage_schedule]
📍 Адреса: м. Кривий Ріг, Вешенська, буд. 8
...
```

### Розширене використання

```
Я: Коли наступне відключення?

Claude: [Викликає get_next_outage]
⏰ Наступне відключення:
  14.11.25 Четвер, 18:00-19:00
  Тип: Точне відключення ✗
```

```
Я: Покажи всі відключення на понеділок

Claude: [Викликає get_outages_for_day з day_of_week="Понеділок"]
📅 День: Понеділок
Відключення (5):
  ✗ 15.11.25 08:00-09:00 (definite)
  ...
```

## Налаштування логування

Для відлагодження ви можете увімкнути детальне логування:

```json
{
  "mcpServers": {
    "electricity-shutdowns": {
      "command": "/path/to/venv/bin/python",
      "args": ["-m", "src.server"],
      "cwd": "/path/to/project",
      "env": {
        "PYTHONUNBUFFERED": "1",
        "LOG_LEVEL": "DEBUG"
      }
    }
  }
}
```

Логи можна переглянути в консолі Claude Desktop (доступ через меню розробника).

## Структура даних

### Конфігурація зберігається в:
```
~/.config/electricity_shutdowns_mcp/
├── config.json          # Адреса та налаштування моніторингу
└── schedule_cache.json  # Кеш графіків відключень
```

### Формат кешу (тримається 1 година):
- `actual_schedules` - Точні графіки на сьогодні/завтра
- `possible_schedules` - Прогнозні графіки на тиждень
- `last_updated` - Час останнього оновлення

## Наступні кроки

Після успішного налаштування, ви можете:
1. Налаштувати автоматичний моніторинг (Фаза 5)
2. Додати підтримку кількох адрес (Фаза 8)
3. Налаштувати локалізацію (Фаза 7)

## Додаткові ресурси

- [MCP Documentation](https://modelcontextprotocol.io/)
- [Claude Code Documentation](https://docs.claude.com/en/docs/claude-code)
- [Project README](README.md)
- [Architecture Documentation](ARCHITECTURE.md)
