# Electricity Shutdowns MCP Server

MCP-сервер для мониторинга графика отключений электроэнергии в Украине (ДТЭК Днепровские электросети). Поддерживатеся пока только Днепропетровская область.

## Описание

Этот MCP-сервер помогает отслеживать график плановых отключений электроэнергии и своевременно уведомляет о предстоящих отключениях. Особенно полезен для планирования зарядки ноутбуков и других устройств.

### Основные возможности

- 🔍 **Проверка графика отключений** по вашему адресу
- ⏰ **Уведомления** за 1 час (настраивается) до отключения
- 📊 **Детект изменений** графика с автоматическими проверками
- 🔋 **Умный расчет** времени зарядки (в разработке)
- 🌐 **Парсинг данных** с сайта ДТЭК в реальном времени

## Требования

- Python 3.10 или выше
- Claude Code CLI
- Интернет-соединение для доступа к сайту ДТЭК

## Установка

### 1. Клонирование репозитория

```bash
cd /path/to/electricity_shutdowns_mcp
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Установка Playwright браузера

```bash
playwright install chromium
```

### 5. Конфигурация Claude Code

См. подробную инструкцию в [CLAUDE_CODE_SETUP.md](CLAUDE_CODE_SETUP.md)

Краткая версия - добавьте сервер в конфигурацию Claude Code:

**macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
**Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
**Linux:** `~/.config/Claude/claude_desktop_config.json`

```json
{
  "mcpServers": {
    "electricity-shutdowns": {
      "command": "/ПОЛНЫЙ/ПУТЬ/К/venv/bin/python",
      "args": ["-m", "src.server"],
      "cwd": "/ПОЛНЫЙ/ПУТЬ/К/ПРОЕКТУ/electricity_shutdowns_mcp",
      "env": {}
    }
  }
}
```

**Важно:** Замените пути на реальные абсолютные пути к вашему проекту.

## Использование

### Настройка адреса

При первом использовании укажите свой адрес:

```
Настрой мой адрес для проверки отключений: город Днепр, улица Набережная Победы, дом 50
```

### Проверка графика

```
Проверь график отключений на сегодня
Когда следующее отключение?
```

### Мониторинг изменений

```
Включи автоматическую проверку графика каждое утро в 8:00
Уведомляй меня за 1 час до отключения
```

## Доступные инструменты (Tools)

### `set_address`
Настраивает адрес пользователя для проверки графика отключений.

**Параметры:**
- `city` (str): Населенный пункт с префиксом (например, "м. Дніпро", "м. Київ")
- `street` (str): Улица с префиксом (например, "Просп. Миру", "Вул. Шевченка")
- `house_number` (str): Номер дома (например, "4", "50а")

**Возвращает:** Подтверждение сохранения адреса

**Важно:** Адрес сохраняется и используется для всех последующих запросов.

### `check_outage_schedule`
Проверяет актуальный график отключений для настроенного адреса.

**Параметры:**
- `include_possible` (bool, optional): Включить прогноз на неделю (по умолчанию False)
- `force_refresh` (bool, optional): Принудительно обновить данные, игнорируя кеш (по умолчанию False)

**Возвращает:**
- Точный график отключений на сегодня/завтра ("Графік відключень:")
- Опционально: прогноз на неделю ("Графік можливих відключень на тиждень:")
- Статистику по типам отключений
- Время последнего обновления

**Кеширование:** Данные кешируются на 1 час для ускорения повторных запросов.

### `get_next_outage`
Находит ближайшее следующее отключение из точного графика.

**Параметры:** Нет (использует настроенный адрес)

**Возвращает:**
- Дата и день недели следующего отключения
- Время начала и конца отключения
- Тип отключения (точное/первые 30 мин/вторые 30 мин)

### `get_outages_for_day`
Получить все отключения для конкретного дня недели.

**Параметры:**
- `day_of_week` (str): День недели (Понеділок, Вівторок, Середа, Четвер, П'ятниця, Субота, Неділя)
- `schedule_type` (str, optional): Тип графика - "actual" (точный) или "possible_week" (прогноз). По умолчанию "actual"

**Возвращает:** Список всех отключений для указанного дня с временем и типами

## Архитектура

```
electricity_shutdowns_mcp/
├── src/
│   ├── server.py           # Основной MCP-сервер
│   ├── parser.py           # Парсер сайта ДТЭК
│   ├── scheduler.py        # Логика мониторинга и уведомлений
│   └── config.py           # Конфигурация и хранение данных
├── tests/
│   ├── test_parser.py
│   └── test_server.py
├── requirements.txt
├── README.md
└── TODO.md
```

## Источник данных

Данные о графике отключений берутся с официального сайта ДТЭК Днепровские электросети:
https://www.dtek-dnem.com.ua/ua/shutdowns

### Типы графиков

Сайт ДТЭК предоставляет два типа графиков:

1. **"Графік відключень:"** (Actual Schedule)
   - Точный график отключений на сегодня и завтра
   - На завтра данные появляются ближе к концу дня
   - Используется для уведомлений и точного планирования
   - Приоритетный источник при запросах "на сегодня"

2. **"Графік можливих відключень на тиждень:"** (Possible Schedule)
   - Прогноз возможных отключений на неделю
   - Менее точный, используется для общего планирования
   - Показывается пользователю только при запросе конкретных дней недели

### Типы отключений

На графике используются разные обозначения:
- ✗ (черный) - **"Світла немає"** - точное отключение
- ⚡ (желтый) - **"Світла не буде перші 30 хв"** - отключение в первые 30 минут часа
- ⚡ (со звездочкой) - **"Світла можливо не буде другі 30 хв"** - возможное отключение во вторые 30 минут
- Серый фон - **"Можливо відключення"** - возможное отключение (из недельного графика)

## Разработка

### Запуск в режиме разработки

```bash
python src/server.py
```

### Тестирование

```bash
pytest tests/
```

## Roadmap

- [x] Базовый парсинг сайта ДТЭК
- [x] Основные MCP tools
- [ ] Умный расчет времени зарядки

## Troubleshooting

### Ошибка подключения к сайту ДТЭК

Если сайт недоступен или изменилась структура страницы, проверьте:
1. Доступность сайта в браузере
2. Актуальность селекторов в `parser.py`

### MCP-сервер не обнаружен в Claude Code

1. Проверьте правильность путей в конфигурации
2. Убедитесь что виртуальное окружение активировано
3. Перезапустите Claude Code

## Лицензия

MIT

## Автор

Yaroslav Yenkala
Bohdan Perchuk

## Поддержка

Для вопросов и предложений создавайте issues в репозитории.

---

**Слава Україні!** 🇺🇦
